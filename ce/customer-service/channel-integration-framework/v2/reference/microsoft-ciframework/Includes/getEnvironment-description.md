Gets the current Unified Interface app and page details. The details include `appid`, `etn', `pageType`, and `recordId` (if available).

[!INCLUDE[footer-include](../../../../../../includes/footer-banner.md)]